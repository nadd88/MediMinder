DB_CONNECTION=sqlite
DB_HOST=localhost
DB_NAME=mediminder
DB_USER=root
DB_PASS=
JWT_SECRET=replace_with_your_own_random_string

# For Render deployment, use DATABASE_URL instead:
# DATABASE_URL=postgresql://user:password@host:port/database